import { Card } from "../ui";

export const ClothCard = ({cloth_url}) => {
  return (
    <Card>      
      <img className="w-full block rounded-b" src= { `../images/image_head_1.jpg` } />
    </Card>
  );
}